Ask AI to explain the code before asking it to change the code.

If you cannot say what `filter`, `setFilter`, and `visibleTasks` do, start there. Then compare the unfinished button with the three finished buttons beside it and follow that same pattern.
